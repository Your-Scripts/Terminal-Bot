{
  "name": "terminal_bot",
  "version": "1.0.0",
  "description": "bot de cmd",
  "main": "bot.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "discord.js": "^12.5.1"
  }
}
